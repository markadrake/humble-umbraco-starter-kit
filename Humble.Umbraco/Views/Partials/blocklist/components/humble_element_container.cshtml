@inherits UmbracoViewPage<BlockListItem>
@{
	if (Model == null)
	{
		return;
	}

	var content = (Humble_Element_Container)Model.Content;
	var settings = Model.Settings;
}

@Html.GetBlockListHtml(content.Content)