@using Humble.Umbraco.UI.Extensions
@using Humble.Umbraco.UI.TagHelpers
@inherits UmbracoViewPage<BlockListItem>
@{
	if (Model == null)
	{
		return;
	}

	var content = (Humble_Ui_RichText)Model.Content;
	var settings = Model.Settings;
}

<humble-parser content="content.Copy" />